---
description: Compare code against specs and update the specs where they no longer match.
---

Use the sync-docu skill to compare the current source code against the
files in specs/ (and any other relevant documentation), and update the
spec files wherever they no longer match the implemented behavior. Report
any cases where the code seems to diverge from documented intent instead
of silently overwriting them.
